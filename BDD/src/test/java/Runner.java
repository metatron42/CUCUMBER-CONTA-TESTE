import org.junit.runner.RunWith;
import cucumber.api.junit.Cucumber;

/**
 * Classe que serve como ponto de entrada para execução dos testes Cucumber.
 * Indica ao JUnit que os testes devem ser executados usando o Cucumber.
 */
@RunWith(Cucumber.class)
public class Runner {
    // Nenhuma lógica adicional nesta classe, pois é principalmente um ponto de entrada para execução de testes.
    
    /**
     * Construtor padrão.
     * Este construtor está vazio, pois não são necessárias inicializações específicas no momento.
     */
    public Runner() {
        // Nenhuma lógica específica neste construtor
    }
}
